
```
echo "# learn_git" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master //M代表重命名,D代表删除
git remote add origin git@github.com:hezongdnf/learn_git.git //origin <name>
git push -u origin master //push推送，fatch拉取
```